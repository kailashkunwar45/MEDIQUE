{
  "name": "mediqueue-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --include=dev",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
