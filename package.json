{
  "name": "mediqueue-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/api && npm install --include=dev && npm run build && cd ../../apps/web && npm install --include=dev && npm run build",
    "start": "node apps/api/dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
